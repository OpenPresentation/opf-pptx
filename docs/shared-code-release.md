# Converter 0.7.0 release gates

Branch `codex/shared-code-release-20260909` prepares converter 0.7.0 from tested integration `b4f4645cdaa2f5eff8a883f68533875d3b63f239`. Core 0.9.0 and renderer 0.7.0 are published and verified through fresh Node 20/24 registry checks. Renderer release PR #11 merged as `f4a1b8e1324cbc182b98d43f16528594c8006f86`. This converter's lockfile now resolves those actual releases, with unrelated dependencies unchanged.

Full local checks pass on Node 20.20.2 and 24.20.0: package validation, all model/corpus/dependency-boundary suites, accepted code layout and 132 XML-character rejections, guarded provenance recovery, fresh packed consumers and actual Edge browser suites. Packed installs byte-match all 20 shipped files, prove the optional renderer can be absent, then install exact registry renderer 0.7.0 for quote/code fixtures. Both runtimes produce `sha512-GyOVAheZsWkr0zDjEnqQQljnIjzKXH2C3no/wJhA4D5VSlXqz4HyyeG+3cBjUYFC94MW0uqYFZsW3TbK9PQmdw==`. npm reports zero audited advisories, 61 dependency signatures and twelve attestations; the converter candidate is not yet published or attested.

`npm run test:packed -- --native` now executes twelve local-Calibri quote and eight local-Courier-New code cases using actual installed converter files and registry predecessors. Both runtimes pass editable PowerPoint save/reopen, six quote deck imports and all 24 original/saved/edited code slide imports with exact source/metadata. Four native tab targets per runtime have maximum error 0.007031 point against the unchanged 0.02-point gate. Native reports bind executable, Windows, fonts and runtime bytes. No proprietary fonts are copied or embedded, and unrelated user presentations stay open.

[Portable evidence](evidence/shared-code-release/summary.json) includes both runtime matrices, native generation/comparison reports and representative inspected wide/portrait SVG/native rasters. Baselines and glyph appearance differ. Native text recovery does not establish lossless styling, font/theme restoration or raster equivalence.

Full final CI and review remain mandatory before an exact-head merge. Verify the merge tree matches the reviewed head, check the new version/tag is unused, and publish `opf-pptx-v0.7.0` through the trusted workflow. The workflow now includes code/provenance tests. Core's strengthened publication helper must then execute immutable code/provenance and native quote/code fixtures against actual registry files on both runtimes. Only afterward refresh editor 0.6.0's lockfile. The complete-set plan and public deployments advance only after every coordinated successor is verified.

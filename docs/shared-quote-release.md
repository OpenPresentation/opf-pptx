# PPTX 0.6.0 release candidate

Historical preparation record: converter 0.6.0 was subsequently published from `898e3c27919a2488e1e3d384168d6b25aae4bd5c`. Continue with the [0.7.0 release gates](shared-code-release.md); candidate status below describes the earlier checkpoint.

Branch `codex/shared-quote-release-20260909` prepares PPTX 0.6.0 with core `^0.8.0` and optional renderer peer `^0.6.0` (development fixture version 0.6.0). PPTX 0.6.0 is unpublished. The original tested integration remains at `829103b67fdc96ae977bbb05f3e94457620645cd` on `codex/shared-quote-integration-20260909`. Core 0.8.0 (`4dc292fa93bee52320bafa3fd0f5b05a2dc0a283`) and renderer 0.6.0 (`7fe9905ad2d8a224efeef51b4a22d0aff0c413fe`) are published and verified from fresh npm installations on Node 20/24.

The refreshed lock resolves both actual published predecessors without unrelated dependency upgrades. A clean npm install, full model/export corpus, five browser suites, metadata/syntax and fresh packed consumers pass on Node 20.20.2 and 24.20.0. The candidate consumer byte-matches every shipped file, verifies export/reimport with the optional renderer absent, then installs registry renderer 0.6.0 and checks shared accepted quote geometry/strict rejection. Both packed runs have identical integrity, zero audited advisories, 61 dependency signatures and twelve attestations. The candidate itself is not yet registry-attested. Portable [Node 20](evidence/pptx-060-packed-node20.json) and [Node 24](evidence/pptx-060-packed-node24.json) reports record exact files and predecessor integrities.

`npm run test:packed -- --native` on Windows runs the same fresh installed candidate through actual PowerPoint. It copies the native harness into the isolated consumer, generates twelve controlled Calibri quote cases, invokes PowerPoint to inspect/edit/save/reopen, compares native rasters, and reimports all saved variants before cleaning up the consumer. It leaves unrelated presentations and PowerPoint open. The driver hashes through .NET so it also works when a child Windows PowerShell inherits PowerShell 7's module path.

This command passes on Node 24 with PowerPoint 16.0: twelve wide/portrait cases retain accepted text/font sizes, glyph containment and body/source separation; twelve title edits survive save/reopen; six deck imports validate and retain every footer line and edit. Both contact sheets were visually reviewed. The [new native comparison](evidence/pptx-060-native/comparison.json), [wide sheet](evidence/pptx-060-native/quote-1280-contact.png) and [portrait sheet](evidence/pptx-060-native/quote-540-contact.png) bind to the installed candidate's runtime, consumer lock and four local Calibri font hashes. The earlier source report remains historical. Measured raster differences are observations without an equivalence threshold; these controlled Latin/Calibri cases do not verify open-font substitution or other native platforms.

Linux CI/publication use the exact Playwright 1.63.0 image already verified by coordinated CI `34399051732`, pin its digest and check the installed test version. The Windows matrix remains on native hosted Windows and installs Chromium separately. Full CI/review remain gates before merge and a fresh `opf-pptx-v0.6.0` publication tag. After publication, verify actual npm integrity/provenance and renew installed artifact checks before updating the editor lock.

Native quote import retains editable text but loses OPF quote structure, typography and readability policy. Native chart geometry and general scalar wrapping remain separate fidelity gaps. There is no claim of arbitrary round-trip or pixel equivalence.
